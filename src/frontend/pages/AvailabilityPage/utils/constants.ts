export const MIN_DURATION = 15; // 15 minutes minimum
export const LONG_PRESS_DURATION = 500; // 500ms for long press

// Universal height constant: pixels per hour
// This MUST be used consistently across all components for alignment
export const PIXELS_PER_HOUR = 100; // 100 pixels = 1 hour

// Maximum heights for containers (based on 9-hour reference)
// Kept for reference but not exported as they're not used
// const MAX_HEIGHT_DESKTOP = PIXELS_PER_HOUR * 9; // 900px for 9 hours
// const MAX_HEIGHT_MOBILE = PIXELS_PER_HOUR * 7.2; // 720px for ~7 hours
